# تاریخچه و حافظه پروژه وندینگ ۷۲۴ (نسخه فلاتر - پنل)

## قوانین پایه ارتباطی
- حذف تعارفات و حواشی، تمرکز ۱۰۰٪ مهندسی.
- هر پاسخ هوش مصنوعی باید به یک اقدام عملی (Actionable) و درخواست نتیجه از کاربر ختم شود.
- حفظ معماری آفلاین-محور (Offline-First).

## فاز ۱ و ۲: آماده‌سازی زیرساخت (تکمیل شده)
- **وضعیت شبکه:** استفاده از حالت TUN در V2RayN تایید شد.
- **محیط توسعه:** نصب و تأیید Flutter SDK, VS Code, افزونه‌های Dart/BLoC/SQLite.
- **زیرساخت بک‌اند:** تنظیم PHP 8.1 و Composer در متغیرهای محیطی ویندوز (Laragon).
- **ابزارهای شبیه‌ساز:** نصب موفق Postman، Packet Sender و DB Browser for SQLite از طریق مخزن Winget.

## فاز ۳: راه‌اندازی پروژه و دیتابیس (در حال انجام)
- انتقال موفق محیط توسعه به VS Code و نصب وابستگی‌ها.
- **اصلاحات معماری نسخه ۵.۱ اعمال شده در دیتابیس:** 
  - تغییر فرمول آدرس‌دهی فیزیکی به `(shelf * 1000) + column`.
  - اضافه شدن فیلدهای `sold_price` و `delivered_at` به `Order_Items`.
  - اضافه شدن `db_version` به `System_Configs`.
  - **باگ‌فیکس:** خطای متد `getVersion` در `sqflite_ffi` با جایگزینی کوئری مستقیم به `sqlite_master` جهت شمارش جداول (۹ جدول) برطرف و سلامت دیتابیس تأیید شد.
  - **تأیید دیتابیس:** اجرای کوئری `sqlite_master` روی ویندوز، ساخت دقیق ۹ جدول معماری را تأیید کرد.
## فاز ۴: پیاده‌سازی State Machine (در حال انجام)
- پیاده‌سازی چرخه ماشین حالت پنل (۶ وضعیت اصلی) با استفاده از الگوی BLoC.
- **تأیید State Machine:** چرخه ۶ وضعیتی BLoC و مکانیزم Timeout/Refund با موفقیت تست و تأیید شد.
## فاز ۵: لایه ارتباطی سخت‌افزار (TCP Client) - (در حال انجام)
- ایجاد کلاس `HardwareClient` بر پایه `dart:io` `Raw Sockets`.
- پیاده‌سازی تایم‌اوت سخت‌افزاری (TimeoutException) جهت انتقال به وضعیت Fatal Error.
- پیاده‌سازی متد پردازش خطای مکانیکی (`H-1001`) جهت قرنطینه رک در جدول `Racks_Inventory`.
- **تست شبکه محلی:** شبیه‌ساز Packet Sender روی پورت 8080 راه‌اندازی و به عنوان Mock Hardware پیکربندی شد.
- **تست جامع ارتباط:** اتصال موفق لایه UI به `HardwareClient` از طریق سوکت TCP و هندل کردن پاسخ موتور (H-1001).
- **تست جامع ارتباط:** خروجی موفق لاگ‌ها و شبیه‌سازی دقیق خطای `H-1001` با استفاده از Packet Sender تایید شد.
## فاز ۶: پیاده‌سازی کوئری‌های دیتابیس (در حال انجام)
- ایجاد لایه `VendingRepository` برای ارتباط مستقیم با جداول SQLite.
- پیاده‌سازی متد `seedDummyData` جهت تزریق دیتای تستی به جداول کاتالوگ و رک‌ها.
- پیاده‌سازی کوئری واقعی `quarantineRack` جهت تغییر وضعیت (`status = 0`) در جدول `Racks_Inventory` هنگام دریافت خطای فیزیکی از برد.
- **تست دیتابیس:** کوئری قرنطینه با موفقیت اجرا و مقدار `status` رک شماره ۸ پس از دریافت خطای موتور در SQLite به `0` تغییر یافت. چرخه (UI -> TCP -> DB) کاملاً عملیاتی است.

## فاز ۷: طراحی رابط کاربری (UI) و صفحات مانیتور (در حال انجام)
- ایجاد ساختار پوشه‌بندی ویژگی‌محور (Feature-First) برای صفحات پنل.
- پیاده‌سازی مسیریاب اصلی رابط کاربری (UI Router) بر اساس Stateهای ۶ گانه ماشین حالت.
- **آماده‌سازی ساختار UI:** مسیرهای پایه برای صفحات مانیتور (`features/panel/presentation/screens/`) ایجاد شد.
## فاز ۷: طراحی رابط کاربری (UI/UX) - (مرحله تصمیم‌گیری بصری)
- توقف کدنویسی جهت نهایی‌سازی ساختار بصری، چیدمان (Layout) و تجربه کاربری (UX) ۵ وضعیت اصلی پنل.
- **تصمیمات UI (بخش اول):** جهت مانیتور دستگاه «عمودی» (Portrait - حالت کیوسک/موبایل) تعیین شد.
- **تصمیمات UI (بخش دوم):** جهت عمودی تایید شد. تغییر استراتژی صفحه انتخاب از «گرید محصولات» به «ورودی کیپد (Numpad)» به دلیل رویت‌پذیری فیزیکی کالاها.
- **منطق سبد خرید (UX):** اعمال محدودیت ظرفیت فیزیکی (Slots) با نمایش تامبنیل. استفاده از شناسه موقت (UUID) برای هر آیتم جهت امکان حذف از سبد.
- **بهینه‌سازی UX کیپد:** دکمه تایید (Enter) حذف شد. جایگزینی با منطق «جستجوی زنده و واکنشی» (Reactive Live Search) جهت تسریع فرآیند خرید و کاهش بار شناختی کاربران در سنین مختلف.
- **بهینه‌سازی UX کیپد (نسخه نهایی):** منطق جستجوی زنده لغو شد. جایگزینی با «منطق تاخیر هوشمند (Smart Debounce)» بر اساس طول ارقام و حداکثر ظرفیت رک‌های دستگاه جهت جلوگیری از پرش‌های بصری و خطای کاربری.
- **بازنگری UX کیپد:** چالش کندی عملکرد کاربران مسن بررسی شد. جایگزینی زمان‌سنج (Timer) با استاندارد صنعتی «طول ثابت ارقام» (Fixed-Length Input) جهت حذف کامل تاخیرها.
- **منطق نهایی کیپد (Hybrid UX):** ترکیب تایمر ۱ ثانیه‌ای + دکمه سبز (تایید چشمک‌زن). حذف الزام پیشوند صفر (بومی‌سازی برای کاربران ایرانی). تایید و نمایش رک به سه روش انجام می‌شود: ۱. فشردن دکمه سبز، ۲. اتمام تایمر، ۳. رسیدن به سقف ارقام مجاز.
- **طراحی بصری (UI Design):** ارتقای وایرفریم شماتیک به رابط کاربری استاندارد و مدرن مبتنی بر Material 3.
- **پیاده‌سازی Selection Screen:** ایجاد ساختار UI شامل کارت محصول واکنش‌گرا، کیپد با منطق Hybrid (تایمر + انیمیشن چشمک‌زن) و تامبنیل‌های سبد خرید.
- **بهبود UX کیپد:** زمان تایمر (Debounce) برای ورود اعداد تک‌رقمی، جهت تطابق با سرعت واکنش سالمندان، از ۱ ثانیه به ۲.۵ ثانیه افزایش یافت.
- **معماری UI:** پیاده‌سازی ساختار Component-Based جهت تفکیک کامل اجزای رابط کاربری (Numpad, ProductCard, Cart).
- **نسبت تصویر (Aspect Ratio):** قفل کردن ابعاد فیزیکی برنامه روی ۹:۱۶ عمودی (Portrait) در تمام پلتفرم‌ها (ویندوز و اندروید) با استفاده از مانیتور مجازی جهت حفظ یکپارچگی طراحی کیوسک.
- **تکمیل معماری Component-Based:** تفکیک کامل لایه UI به کامپوننت‌های مستقل (Header, CartStrip, CheckoutBar, Numpad, ProductCard). فایل `selection_screen` صرفاً به عنوان هماهنگ‌کننده (State Orchestrator) ایفای نقش می‌کند.
- **باگ‌فیکس UI:** رفع خطای RenderFlex Overflow در حالت ۹:۱۶ با جایگزینی پدینگ‌های ثابت با مقادیر منعطف و استفاده از `FittedBox` در کامپوننت‌های Numpad، ProductCard و CheckoutBar.
- **بهبود واکنش‌گرایی (Responsiveness):** حذف مانیتور مجازی (AspectRatio 9:16) و آزادسازی ابعاد صفحه برای تطبیق خودکار با مانیتورهای عمودی.
- **ارتقای UX کارت محصول:** جایگزینی متن راهنما با حالت اسکلتون (Skeleton Loading) جهت نمایش قالب‌بندی استاندارد پیش از انتخاب رک.
- **بهینه‌سازی کیپد (Numpad):** رفع مشکل رندر نشدن دکمه‌های تایید/حذف با استفاده از کامپوننت `Material`. تبدیل کادر نمایش اعداد به دو مربع مجزا با انیمیشن تغییر رنگ کادر (سبز شدن) به هنگام ورود ارقام.
- **بهبود UI کیپد:** پیاده‌سازی منطق `isInputConfirmed` جهت اجرای انیمیشن ادغام کادرها به یک کادر سبز رنگ در زمان تایید نهایی ورودی‌های تک‌رقمی (اتمام تایمر یا فشردن دکمه سبز).
- **باگ‌فیکس انیمیشن UI:** رفع خطای `isNonNegative` و `RenderFlex` با جایگزینی منحنی `easeInOutBack` با منحنی‌های خطی‌تر، مدیریت Overflow محتوای متنی در زمان کوچک شدن کادر، و محو کردن کامل حاشیه‌ها و سایه‌ها برای کادر مخفی.
- **بهبود UI کارت محصول:** تغییر رفتار نمایشی خطا (رک نامعتبر/ناموجود). استفاده از ساختار لایه‌بندی (`Stack`) جهت نمایش خطا به صورت Overlay روی قالب اسکلتونی به منظور حفظ یکپارچگی ابعاد و چیدمان.
- **تنظیمات ظاهری (UX Tweaks):** بررسی تضاد شناختی بین اسکلتون و پیام خطا. اصلاح شفافیت لایه Overlay خطا (Alpha = 0.3) جهت رویت‌پذیری قالب اسکلتونی در زیر پیام.
- **اصلاحات UI و تجربه کاربری:** جایگزینی ترکیب «اسکلتون + خطا» با الگوی استاندارد «حالت غیرفعال» (Disabled Placeholder) برای جلوگیری از تضاد شناختی.
- **تایپوگرافی و بومی‌سازی:** افزودن فونت فارسی «وزیر» (Vazir) به عنوان فونت پایه سیستم. کاهش سایز (FontSize) و ضخامت (FontWeight) متون جهت ایجاد ظاهری مدرن‌تر و جلوگیری از خستگی چشم کاربر.
- **بومی‌سازی پیشرفته (تایپوگرافی):** جایگزینی فونت پایه با نسخه `Vazir-Bold-FD-WOL.ttf` (Farsi Digits) جهت تبدیل کاملاً خودکار تمامی اعداد انگلیسی (در کیپد، قیمت‌ها و سبد خرید) به ارقام استاندارد فارسی.
- **اصلاح نهایی کارت محصول:** تعیین حالت پیش‌فرض روی ساختار اسکلتون (Skeleton) و حذف کامل پس‌زمینه و حاشیه‌ها در زمان نمایش پیغام خطا جهت ایجاد ظاهری مینیمال و یکپارچه.
- **بهبود UX سبد خرید (Cart Strip):** اضافه شدن نشانگر بصری (Badge) حذف به صورت آیکون ضربدر قرمز در بالا سمت چپ آیتم‌های سبد خرید. ادغام منطق لمس برای کل مساحت تصویر و آیکون جهت تسهیل فرآیند حذف کالا.
- **استانداردسازی مالی و تایپوگرافی (RTL):** اعمال چیدمان سراسری راست‌به‌چپ (RTL) در فلاتر. طراحی اکستنشن `CurrencyFormatter` جهت درج خودکار کامای هزارگان و قرارگیری صحیح کلمه «ریال» پس از عدد. قفل کردن Numpad در حالت LTR جهت حفظ ساختار استاندارد کیبورد عددی.
- **مکانیزم احراز هویت تکنسین (Admin Login):** پیاده‌سازی دروازه ورود دو مرحله‌ای جهت جلوگیری از دسترسی تصادفی کاربران: ۱. ورود کد رزرو شده (`00`)، ۲. نگه داشتن ۳ ثانیه‌ای (Long-Press) دکمه تایید. حذف ایده زمان تصادفی جهت حفظ پیش‌بینی‌پذیری و جلوگیری از خستگی اپراتور.
- **احراز هویت تکنسین (Admin Login):** پیاده‌سازی مکانیزم کد رزرو شده (`00`) به همراه نگه داشتن ۳ ثانیه‌ای دکمه تایید جهت تغییر فاز کیپد به Password Mode (ورود پین‌کد ۴ رقمی). ثبت رویداد `OP-1001` (ورود به حالت تعمیرات) در صورت صحت رمز عبور.
- **باگ‌فیکس UI:** رفع خطای RenderFlex Overflow در حالت Admin Mode (پین‌کد ۴ رقمی) با استفاده از `FittedBox` جهت مقیاس‌پذیری خودکار کادرهای نمایش رمز عبور.

چک‌پوینت یکپارچه معماری و وضعیت فعلی پروژه (نسخه ۵.۲ - پایان فاز UI کیپد و ادمین)

این مستند حاوی دقیق‌ترین وضعیت از تمامی متغیرها، کلاس‌ها، تنظیمات و روابط بین اجزای سیستم تا این لحظه است و به عنوان مرجع توسعه برای گام‌های بعدی عمل می‌کند.
۱. پیکربندی محیط و تنظیمات گلوبال (Global Settings)

    فریم‌ورک و زبان: Flutter (توسعه چندسکویی با خروجی یکپارچه برای Windows و Android) / Dart.

    چیدمان و راست‌چین‌سازی (RTL): کل اپلیکیشن در فایل main.dart درون ویجت Directionality(textDirection: TextDirection.rtl) رپ (Wrap) شده است تا چیدمان بر اساس استانداردهای زبان فارسی تنظیم شود.

    تایپوگرافی و فونت: استفاده از فونت آفلاین Vazir-Bold-FD-WOL.ttf (با پشتیبانی از اعداد فارسی). تم سراسری MaterialApp روی fontFamily: 'Vazir' تنظیم شده است.

    ابزار قالب‌بندی مالی (Extension): فایل lib/core/utils/extensions.dart شامل اکستنشن CurrencyFormatter روی نوع int است که فرمت سه‌رقم جداکننده (کاما) و پسوند «ریال» را تولید می‌کند (مثال: ۲۵,۰۰۰ ریال).

۲. ساختار دیتابیس محلی (SQLite / Repository)

دیتابیس آفلاین با معماری توزیع‌شده و مرجعیت دوگانه (Local/Remote) جهت حفظ عملکرد بی‌وقفه در زمان قطعی اینترنت دارای جداول زیر است:

    Products_Catalog: id، name، image_path، base_price (فقط ماهیت کالا را می‌شناسد و درگیر سخت‌افزار نیست).

    Machine_Layout: shelf_number، columns_count (فرمول آدرس‌دهی فیزیکی: (shelf * 1000) + column).

    Racks_Inventory: rack_number، physical_address، product_id، current_price، stock، status (مغز پردازشی رک‌ها).

    Orders: id (UUID)، total_amount، cart_capacity، pos_status، timestamp.

    Order_Items: id، order_id، rack_number، product_id، delivery_status، sold_price (ثبت دقیق قیمت لحظه فروش).

    System_Logs: id، log_type (SOFT/HARD/OP)، error_code، description، timestamp، is_synced.

    System_Configs: machine_id، server_base_url، last_sync، board_ip، maintenance_mode، db_version.

۳. ماشین حالت و منطق BLoC (State Machine)

حلقه بسته مدیریت وضعیت کلاینت بر اساس ۵ وضعیت کاملاً ایزوله حرکت می‌کند:

    States:

        IdleState (آماده‌به‌کار و اسکرین‌سیور)

        SelectionState (انتخاب و درج در سبد)

        PaymentState (قفل UI و ارتباط با پوز)

        MachineDispensingState (ارسال به برد و انتظار پاسخ)

        FatalErrorState (تایم‌اوت سخت‌افزار)

        CompletionState (نتیجه، پردازش خطا و قرنطینه)

    Events: ScreenTouched، PaymentInitiated، PaymentSuccess، HardwareResponded، HardwareTimeoutOccurred، ResetMachine.

۴. لایه شبکه و سخت‌افزار (TCP/IP Socket)

    فایل ارتباطی: lib/core/network/hardware_client.dart

    عملکرد: ارسال درخواست‌های تحویل با فرمت استاندارد JSON به صورت پردازش دسته‌ای (Batch Processing) به TCP Server برد الکترونیکی (پورت ۸۰۸۰).

    مدیریت خطای سخت‌گیرانه: در صورت دریافت پاسخ MOTOR_JAM (کد خطای H-1001)، تابع قرنطینه در VendingRepository فراخوانی شده و فیلد status رک معیوب در دیتابیس False می‌شود تا از سیستم فروش حذف گردد.

۵. معماری رابط کاربری (UI Component Tree)

صفحه انتخاب کالا (SelectionScreen) به عنوان هماهنگ‌کننده (Orchestrator) عمل کرده و Stateها را بین ۵ ویجت مستقل (موجود در lib/features/panel/presentation/widgets/) توزیع می‌کند:

الف) متغیرهای کنترلی در SelectionScreen:

    _currentInput (String): ذخیره ورودی لحظه‌ای کاربر.

    _isInputConfirmed (bool): پرچم تایید نهایی برای تغییر رنگ و انیمیشن کادرها.

    _isAdminMode (bool): پرچم تغییر فاز کیپد از ۲ رقمی (خرید) به ۴ رقمی (رمز عبور).

    _cart (List): آرایه نگهداری اقلام موقت سبد خرید (دارای شناسه یکتای موقت id).

    _maxCartCapacity (int): ظرفیت مجاز سبد دستگاه (مثلاً ۵).

    _adminHoldTimer (Timer): تایمر ۳ ثانیه‌ای برای نگه داشتن دکمه تایید جهت ورود تکنسین.

    _debounceTimer (Timer): تایمر ۲.۵ ثانیه‌ای برای تایید خودکار رک‌های تک‌رقمی.

    _pulseAnimation (Animation): کنترل انیمیشن چشمک‌زن دکمه سبز.

ب) کامپوننت NumpadWidget (کیپد هوشمند):

    درون Directionality(textDirection: TextDirection.ltr) قفل شده تا اعداد و چینش کیبورد استاندارد بماند.

    در حالت عادی (خرید): ۲ کادر نمایش می‌دهد. با زدن اولین رقم کادر دوم منتظر می‌ماند. با اتمام تایمر (۲.۵ ثانیه) یا زدن دکمه سبز یا ورود رقم دوم، کادر دوم در کادر اول ادغام شده و کادر سبز می‌شود (Confirmed).

    در حالت ادمین (_isAdminMode == true): با کمک FittedBox، به ۴ کادر کوچک همراه با آیکون قفل تبدیل می‌شود و ورودی‌ها را به صورت ستاره (*) نمایش می‌دهد.

ج) کامپوننت ProductCardWidget (کارت محصول):

    حالت پیش‌فرض (خالی/در حال جستجو): نمایش فرمت اسکلتون (Skeleton) ثابت.

    حالت خطا (قرنطینه/ناموجود/رک نامعتبر): اسکلتون حذف شده و پیام خطا بدون بک‌گراند روی کارت سفید رندر می‌شود (Disabled State با آیکون قرمز).

    حالت موفق: عکس محصول، نام و قیمت فرمت‌شده (toRial) رندر می‌شود. اگر ظرفیت سبد پر شود، دکمه افزودن غیرفعال می‌گردد.

د) کامپوننت CartStripWidget (نوار سبد خرید):

    نمایش تامبنیل‌ها در ۵ جایگاه (Slots).

    دارای یک نشانگر (Badge) شامل آیکون ضربدر قرمز در بالا-سمت چپ (موقعیت‌دهی با Positioned و Stack). کل مساحت کارت قابل لمس (Tap) برای حذف کالا از آرایه _cart است.

هـ) کامپوننت‌های CheckoutBarWidget و HeaderWidget:

    هدر: شامل پشتیبانی، نام دستگاه و تاریخ.

    نوار پرداخت: شامل محاسبه totalPrice از روی آرایه سبد خرید، نمایش با فرمت مالی (toRial) درون FittedBox و دکمه پرداخت که در صورت خالی بودن سبد، غیرفعال (null) است.

۶. سناریوهای لبه و امنیتی پیاده‌سازی شده

    احراز هویت تکنسین (Admin Login): کلاینت به هیچ وجه کد 00 را در دیتابیس جستجو نمی‌کند (به عنوان کد رزرو شده). با ورود 00 و لمس ممتد (TapDown) دکمه سبز به مدت ۳ ثانیه، تایمر _adminHoldTimer منقضی شده و کیپد به فاز Password Mode وارد می‌شود.

    لاگ‌گیری عملیاتی (OP-1001): در صورت ورود رمز صحیح تکنسین (1234)، رویداد OP-1001 (ورود به حالت تعمیرات جهت رهگیری فیزیکی اپراتور) در سیستم ثبت می‌شود.

    لغو عملیات (Cancel/Backspace): دکمه قرمز به صورت هوشمند، کاراکترها را تک‌به‌تک (در حالت ادمین) پاک می‌کند و در حالت عادی باعث ریست کامل فرم می‌شود.

- **بهبود معماری UI (مانیتورهای بزرگ):** رفع خطای کشیدگی (Stretching) کیپد و کارت محصول در رزولوشن‌های بالای عمودی. اختصاص ارتفاع ثابت (Fixed Height = 480px) به بخش تعاملی و اضافه شدن کامپوننت `AdSliderWidget` (به صورت Expanded) جهت استفاده از فضاهای خالی مانیتور برای نمایش تبلیغات و تراز شدن کامل المان‌ها.
- **باگ‌فیکس UI:** رفع خطای RenderFlex Overflow در رزولوشن‌های پایین با جایگزینی `SizedBox` ثابت با ترکیب `Flexible` و `ConstrainedBox(maxHeight: 480)`. این معماری از کشیدگی در مانیتورهای بزرگ جلوگیری کرده و در مانیتورهای کوچک به صورت واکنشی مقیاس‌پذیر (Scale-Down) می‌شود.
* **معماری ریسپانسیو درصدی:** کل صفحه اصلی Selection Screen به ساختار منعطف بر پایه Expanded تبدیل شد تا مانیتورهای عمودی بزرگ بدون هیچ‌گونه مشکل کشیدگی (Stretching) رندر شوند (۱۰٪ هدر، ۱۰٪ اسلایدر، ۴۰٪ بخش تعاملی شامل کیپد و کارت کالا، ۱۵٪ راهنما، ۱۵٪ سبد خرید، ۱۰٪ پرداخت). ویجت GuideWidget نیز جهت راهنمایی بصری خریداران اضافه گردید.
* **اصلاحات UI:** ترتیب چیدمان بخش تعاملی اصلاح شد (با توجه به محیط RTL، برای قرارگیری کیپد در سمت راست و کارت در چپ، جایگاه آن‌ها در Row معکوس شد). پنل تبلیغات به یک نگهدارنده متنی ساده (جای تبلیغات شما) تغییر یافت. ابعاد آیکون‌ها و متون در کامپوننت راهنما (Guide Widget) برای زیبایی بیشتر کوچک‌تر شدند.
* **بهینه‌سازی سایز Numpad:** ابعاد کادرهای نمایش شماره رک و پین‌کد ادمین به همراه سایز فونت آن‌ها در `numpad_widget.dart` جهت ظرافت بیشتر و تناسب با مانیتورهای بزرگ، دقیقاً ۳۰ درصد کاهش یافت.
* **سیستم تبلیغات هوشمند (Ad Slider):** ویجت تبلیغات از حالت استاتیک خارج و به یک موتور چرخش هوشمند مبتنی بر وزن (Weighted Selection) ارتقا یافت. این سیستم ساختار دیتابیس آینده (is_active, display_weight) را شبیه‌سازی می‌کند و در هر بازه زمانی (مثلاً هر ۵ ثانیه)، تبلیغ بعدی را بر اساس درصد شانسی که ادمین به آن اختصاص داده، رندر می‌کند.
* **اصلاحات UI (پنل تبلیغات تمام‌عرض):** ویجت `AdSliderWidget` با حذف انحنای حاشیه‌ها (Border Radius) و مارجین‌های افقی، به صورت تمام-عرض (Edge-to-Edge) تغییر شکل داد تا در مانیتورهای صنعتی ظاهری یکپارچه‌تر با استانداردهای دیجیتال ساینج (Digital Signage) داشته باشد.
* **اصلاح باگ UI:** باگ قفل شدن مقیاسِ دکمه تایید سبز رنگ (ماندن در حالت سایز کوچک پس از اتمام انیمیشن Pulse) در Numpad با اصلاح مقادیر `_pulseController.reset()` در توابع کنترلی `selection_screen.dart` به طور کامل برطرف شد.
* **اصلاح تجربه کاربری (UX) در راهنما:** متون و آیکون‌های ویجت `GuideWidget` به‌روزرسانی شدند تا امکان خرید چندتایی (Multi-item Cart) و تسویه یکجای سبد خرید به صورت واضح‌تری به خریدار القا شود.
* **فاز پنل تکنسین (Admin Dashboard):** صفحه پایه `AdminDashboardScreen` طراحی و در ساختار پروژه قرار گرفت. منطق مسیریابی (Routing) در کنترلر کیپد تنظیم شد تا پس از وارد کردن کد محرمانه تکنسین و لاگین موفق (`OP-1001`)، دستگاه از حالت وندینگ خارج شده و به داشبورد مدیریتی منتقل شود.
* **طراحی پنل مدیریت ادمین (سطوح دسترسی و کارت‌های لیستی):** اضافه شدن لایه دوم احراز هویت با فرم ورود شامل منوی کشویی انتخاب نقش (`اپراتور`، `مالک`، `فنی`) و فیلد ورود رمز عددی ۶ رقمی (هاردکد شده برای فاز ارزیابی). تعریف دقیق سطوح دسترسی مبتنی بر نقش‌ها روی ۱۰ کارت مدیریتی لیستی با آیکون‌های ساده خطی (شامل: ۱. لیست محصولات، ۲. افزودن محصول، ۳. تنظیمات لایوت رک، ۴. تنظیمات شبکه/سرور، ۵. معرفی دستگاه پوز، ۶. بخش تبلیغات، ۷. آمار فروش، ۸. لاگ‌های سیستم پنل، ۹. خطا/لاگ برد الکترونیکی، ۱۰. پنل تنظیمات اس‌ام‌اس).
* **پیاده‌سازی لایه دوم احراز هویت ادمین و کارت‌های لیستی:** فرم ورود دوم با منوی کشویی انتخاب نقش (`اپراتور`، `مالک`، `فنی`) و تایید رمز ۶ رقمی هاردکد شده پیاده‌سازی شد. بخش مدیریتی ادمین به صورت لیست عمودی با آیکون‌های ساده خطی درآمد و ۱۰ کارت ماژولار سیستم بر اساس سطح دسترسی هر نقش به صورت داینامیک فیلتر و نمایش داده می‌شوند.
* **پیاده‌سازی فرم ورود دوم ادمین (Secondary On-Screen Numpad):** صفحه `AdminDashboardScreen` مجهز به سیستم لاگین دوم مبتنی بر تاچ‌اسکرین شد. با توجه به عدم وجود کیبورد فیزیکی در پنل‌های لمسی، یک Numpad اختصاصی کوچک برای ورود پین‌کد ۶ رقمی (با نمایش ستاره‌ای) در کنار منوی کشویی انتخاب نقش تعبیه شد. لیست ۱۰ کارت مدیریتی به صورت لیستی با آیکون خطی، بر اساس سطح دسترسی نقش انتخابی (`اپراتور`، `مالک`، `فنی`) به صورت داینامیک فیلتر می‌شوند.
* **اصلاحات UI و باگ‌های لاگین:** رفع باگ Ghost Tap (ارزیابی اشتباه رمز بلافاصله پس از تغییر فاز ۳ ثانیه‌ای کیپد)، رفع بیرون‌زدگی ۳ دکمه پایین در فرم ورود دوم با استفاده از `shrinkWrap`، و اضافه شدن ماژول «مدیریت کاربران» با دسترسی انحصاری برای مالک و تکنسین.
* **اصلاحات UI (ویجت Numpad):** ظاهر Numpad اصلی دستگاه (فایل `numpad_widget.dart`) با تنظیم نسبت ابعاد دکمه‌ها (`childAspectRatio: 1.4`)، اضافه کردن پدینگ افقی و کاهش سایز باکس‌های ورود و فونت‌ها، از حالت کشیده و درشت خارج شد و ظاهری مینیمال، مدرن و متناسب با استانداردهای کیوسک لمسی پیدا کرد.
* **فاز ماژول‌های ادمین (پاپ‌آپ افزودن محصول):** ماژول «افزودن محصول» مطابق با معماری پاپ‌آپ‌های داشبورد ادمین طراحی و به کامپوننت `AddProductDialog` ارتقا یافت. خطاهای مربوط به اکستنشن مالی (`toRial`) برطرف شد. این فرم کاملاً از محیط سیستم‌عامل ایزوله است (کیبورد سیستم باز نمی‌شود) و از یک کیبورد لمسی درون‌برنامه‌ای (`PersianKeyboardWidget`) برای درج نام و قیمت فرمت‌بندی‌شده پشتیبانی می‌کند.
* **فاز ماژول‌های ادمین (پاپ‌آپ افزودن محصول):** ماژول «افزودن محصول» مطابق با معماری پاپ‌آپ‌های داشبورد ادمین طراحی و به کامپوننت `AddProductDialog` ارتقا یافت. خطاهای مربوط به اکستنشن مالی (`toRial`) برطرف شد. این فرم کاملاً از محیط سیستم‌عامل ایزوله است (کیبورد سیستم باز نمی‌شود) و از یک کیبورد لمسی درون‌برنامه‌ای (`PersianKeyboardWidget`) برای درج نام و قیمت فرمت‌بندی‌شده پشتیبانی می‌کند.
* **اصلاحات UI (کیبورد اختصاصی فارسی):** کیبورد لمسی ماژولار (`PersianKeyboardWidget`) برای جلوگیری از بیرون‌زدگی (Overflow) در ابعاد مختلف پاپ‌آپ‌ها، با استفاده از سیستم درصددهی و ویجت `Expanded` کاملاً ریسپانسیو شد. برای حل تضاد متون راست‌چین (RTL) با استاندارد کیبورد QWERTY فارسی، کیبورد درون قالبِ `Directionality(LTR)` قفل شد تا کلیدها دقیقاً مشابه صفحه کلید سخت‌افزاری کامپیوتر (از 'ض' در چپ تا 'چ' در راست) رندر شوند و ردیف اعداد (1 تا 0) نیز در بالاترین ردیف قرار گرفت.
* **پایدارسازی دارایی‌ها (Local Asset Management):** برای تضمین یکپارچگی سیستم آفلاین، با کمک افزونه `path_provider` مکانیزمی در `VendingRepository` پیاده‌سازی شد که تصاویر انتخابی در فرم «افزودن محصول» را از پوشه ناپایدار (Temp) سیستم‌عامل استخراج کرده، با یک نام زمان‌بندی‌شده و منحصربه‌فرد تغییر نام می‌دهد و در یک پوشه پایدار (`vending_assets/products` در فولدر Documents دستگاه) کپی می‌کند.
* **بهینه‌سازی رابط کاربری دیالوگ (Add Product UX):** دکمه صریح **«ثبت و افزودن محصول»** به فرم اضافه گردید. منطق رویداد فرم به گونه‌ای مهندسی شد که پس از اجرای موفق عملیات ذخیره‌سازی فایل و درج در دیتابیس (`Products_Catalog`)، دیالوگ به جای بسته شدن، کاملاً Reset و پاک‌سازی شود تا ورود پیاپی چندین محصول با سرعت بالا (بدون نیاز به باز و بسته کردن مکرر پاپ‌آپ) میسر گردد.
* **ساختار دیتابیس (Products_Catalog):** فیلد `is_active` (از نوع Boolean/Integer با پیش‌فرض ۱) به جدول محصولات اضافه شد تا سناریوی «حذف نرم» (Soft Delete) به جای حذف قطعی از دیتابیس پیاده‌سازی شود و یکپارچگی گزارشات فروش حفظ گردد. 
* **الگوی ویرایش درجا (Accordion Inline-Editing):** کامپوننت هوشمند `ProductListDialog` توسعه یافت. با فشردن دکمه ویرایش روی هر آیتم، ردیف به صورت آکاردئونی گسترش یافته، اطلاعات به فرمت فیلد درآمده و کیبورد `PersianKeyboardWidget` در پایین دیالوگ ظاهر می‌شود. این الگو باعث حفظ کانتکست (Context Retention) و افزایش سرعت اپراتور می‌شود.
* **الزام سخت‌گیرانه دیتابیس (Dependency Check):** کوئری `isProductInActiveRack` به `VendingRepository` اضافه شد. سیستم پیش از رندر کردن دکمه «حذف»، دیتابیس `Racks_Inventory` را بررسی می‌کند و در صورتی که آن محصول در رکی با وضعیت فعال و موجودی (`stock > 0`) قرار داشته باشد، دکمه حذف به صورت خودکار غیرفعال (Disabled) می‌گردد تا از خطای عملکردی جلوگیری شود.
* **مایگریشن دیتابیس (SQLite Migration):** جهت اعمال تغییرات ساختاری در معماری داده‌ها (افزودن فیلد `is_active` به کاتالوگ محصولات)، نسخه دیتابیس در `database_helper` از ۱ به ۲ ارتقا یافت و متد `onUpgrade` جهت دراپ و ساخت مجدد جداول برای همگام‌سازی ساختار لوکال سیستم توسعه با معماری جدید کدها افزوده شد.
